# Required: EVM private key (0x-prefixed hex)
WALLET_PRIVATE_KEY=0x...

# Optional: Override API endpoint (default: https://ai-inferx.vercel.app)
# INFERX_ENDPOINT=https://ai-inferx.vercel.app

# Optional: Persisted balance auth token (overrides ~/.inferx/auth_token file)
# INFERX_AUTH_TOKEN=0x...
